# Desktop profile configuration (Electron/Desktop version, maps to APP_PROFILE=local)
VITE_APP_PROFILE=local
VITE_AUTH_DISABLED=true
# API base uses same-origin /api in desktop mode (frontend served by backend)
# Desktop mode does not need VITE_API_BASE_URL because requests use same-origin /api
